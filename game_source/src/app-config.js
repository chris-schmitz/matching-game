// come back and build in some logic for setting the keypad value
// from the build scripts so you don't have to remember to do it manually
// at buildAndPack time


const config = {
    showKeypad: false
}

module.exports = config
